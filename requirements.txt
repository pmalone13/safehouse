# queue_db.py, coordinator.py, and twilio_client.py use only the Python
# standard library, on purpose: this VM has 414MB RAM total, so every
# dependency is a real cost here. The Gmail API pieces below are the
# first real exception -- there's no reasonable stdlib-only way to do
# OAuth + the Gmail API, and bayhouse's own proven use of these same
# packages hasn't been a resource problem there.
google-auth
google-auth-oauthlib
google-api-python-client
